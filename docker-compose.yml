services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: always
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=example
      - POSTGRES_DB=job
    ports:
      - "5425:5432"
    volumes:
      - ./db-volumes/database/postgres:/var/lib/postgresql/data
  vacancy_service:
    container_name: vacancy_service
    restart: always
    build:
      context: ./vacancy_service
      dockerfile: Dockerfile
    expose:
      - "5003"
    ports:
      - "127.0.0.1:5003:5003"
    volumes:
      - ./vacancy_service:/app
      - ./var/log:/var/job/log
    environment:
      - PORT=5003
      - DATABASE_URL=postgresql://root:example@postgres:5432/job?schema=public
      - LOG_LEVEL=debug
    depends_on:
      - postgres
networks:
  network:
    driver: bridge
